<%= form_for [@product, @comment] , remote: true do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<%= f.text_area :content, placeholder: "Nhap vao comment" %>
	<%= f.submit "Comment", class: "btn btn-primary" %>
<% end %>